@model List<ResultSocialMediaDto>

<div class="header-left-social">
    <ul class="header-social">
        @foreach (var item in Model)
        {
            <li><a href="@item.Url"><i class="@item.Icon"> @item.Title</i></a></li>
        }        
    </ul>
</div>